const usersReducer = (state = [], action) => {
    // reducer logic for the 'users' module
    switch (action.type) {
      // handle different action types and update the state accordingly
      default:
        return state;
    }
  };
  
  export default usersReducer;
  