const roomsReducer = (state = [], action) => {
    // reducer logic for the 'rooms' module
    switch (action.type) {
      // handle different action types and update the state accordingly
      default:
        return state;
    }
  };
  
  export default roomsReducer;
  